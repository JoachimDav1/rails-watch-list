<div class="nav-bar">
  <%= link_to "ADD NEW LIST  ?????", new_list_path, class: "btn btn-outline-primary" %>
</div>

<div class="banner"  >
<h1>WELCOME TO THE MOVIE SHOW</h1>
</div>


<div class="nav-bar">
  <h2>My list</h2>
  <%= link_to "ADD NEW LIST  ?????", new_list_path, class: "btn btn-outline-primary" %>
</div>
<div class="lists-list">
  <% @lists.each do |list| %>
  <div>
    <div class="list-item">
     <%= link_to list.name, list_path(list.id), class: "text-decoration-none color-white" %> 
    </div>
  </div>
  <% end %>
</div>

