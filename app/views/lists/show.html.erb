<div class="banner"  >
  <h1><%= @list.name %></h1>
</div>

<div class="new-bookmark-form">
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <h3> Add a new movie to this list 🦾</h3>

    <div class="form-item">
      <%= f.input :movie_id, collection: Movie.all, label: "Select Movie" %> 
      <%= f.input :comment %>  
    </div>

    <div class="form-button">
      <%= f.button :submit, class: "btn btn-outline-primary w-100" %> 
    </div>
  <% end %>
</div>

<%= link_to "BACK TO THE LISTS LIST", lists_path, class: "btn btn-outline-primary m-4 " %>

<div class="cards">
  <% @bookmarks.reverse.each do |bookmark| %>
    <% movie = Movie.find(bookmark.movie_id) %>
    <div class="card">
      <img src="<%= movie.poster_url %>" alt="<%= movie.title %>">
      <h2><%= "#{movie.title} • #{movie.rating}⭐" %></h2>
      <h6><%= movie.overview %></h6>
      <p><%= bookmark.comment %></p>
      <%= link_to 'delete', bookmark_path(bookmark.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure???" }, class: "btn btn-outline-danger" %>
    </div>
  <% end %>
</div>
